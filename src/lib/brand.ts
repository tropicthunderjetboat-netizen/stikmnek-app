/** StikmNek app mark (teal “S” icon) — served from /public */

/** Full-resolution mark (PWA / og / large UI). */
export const APP_ICON = '/app-icon.png';

/** Compact mark for in-feed / nav (~72px CSS) — much smaller download. */
export const APP_ICON_192 = '/app-icon-192.png';
